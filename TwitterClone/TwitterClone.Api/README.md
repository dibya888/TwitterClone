\# Question



Sir, I noticed that my Swagger response for `GET /api/Twitter` is showing `authorId`, `content`, and also the `BaseEntity` properties like `id`, `createdAt`, `modifiedAt`, `createdBy`, and `modifiedBy`.



But in your example, I think the response only showed `userId` and `content`.



Is this normal because `Tweet` inherits from `BaseEntity`, or did I miss something?

